node_modules
dist
dist-ssr
.DS_Store
*.local

# Per-user Claude Code settings
.claude/settings.local.json

# Lossless masters for the tracks, if they are ever put back: about 200 MB of
# FLAC. What ships is public/audio/*-final.mp4 — the couple's own trimmed cuts,
# 3.4 MB for the four of them, and committed.
src/media/
